# Handoff — 2026-07-17 (session cut short: battery)

## State of the repo
- Branch `develop`, synced with `origin/develop`. Working tree was CLEAN — everything from today is already in these two pushed commits:
  - `60f0364` (06:39) — sales invoice: restore Cancel/Delete actions in desk UI (removed 29 lines from `public/js/sales_invoice.js` that hid them)
  - `783f383` (08:19) — "all changes", 24 files, +976/−173:
    - **GLMI return formats**: new dedicated Return print formats (A4 + Half-A4) — `grihalaxmi_invoice_a4_return`, `grihalaxmi_invoice_half_a4_return` doctypes + `grihalaxmi_invoice_return.html`, `grihalaxmi_invoice_half_return.html`; tweaks to normal GLMI formats
    - **Report FY filters**: running-FY defaults + date autofill on invoice_activity, materialized, materialized_return, sales_register reports
    - **Numbering**: `numbering_configuration.js/.py`, `numbering_segment.json`, `Override/naming_series.py` (+89)
    - **Misc**: new `public/js/journal_entry.js`, `print_count.py` fix, `nepal_gas_invoice_half.html` Amount-In-Words return fix, numbering docs line

## What we were doing: tracing the "Legacy numbers (before cut-over)" feature
User was reviewing/confirming the legacy cut-over code path in Numbering Configuration
(the section with "Copy Legacy Number Upto" + "Legacy Source Field" on the form).

### Effect chain (save → number lands on the doc)
1. `hooks.py:277-278` — wildcard `validate` + `before_save` → `apply_engine_numbering`
2. `custom_code/Override/naming_series.py:2733` `apply_engine_numbering` → `:2746` calls `set_custom_branch_name(doc)`
3. `naming_series.py:2467` `set_custom_branch_name` → `:2524-2526` `number = _build_from_segments(doc, rule); doc.set(target, number)` ← **effect point**
4. `naming_series.py:2287-2293` inside `_build_from_segments`: if doc date ≤ `legacy_upto`, number is **COPIED** from `legacy_source_field` (+ amendment suffix) instead of generated. Same check duplicated at `:2049` (preview) and `:2621` (series revert path).
5. Validation: `doctype/numbering_configuration/numbering_configuration.py:81` `validate_legacy_cutover()`
6. Dropdown options (why `base_in_words` shows up): `numbering_configuration.js:226-232` — any text-like field (Data/Small Text/Text/Long Text/Text Editor) qualifies.

### ⚠ OPEN FLAG — check before moving on
A screenshot of the live form showed a rule with:
- `Copy Legacy Number Upto = 2026-07-15`
- `Legacy Source Field = base_in_words`

If that's a real saved Numbering Configuration record (not a test), any document backdated
≤ 2026-07-15 will get its voucher number copied from the AMOUNT-IN-WORDS text.
**Verify the record on the site and clear/fix the Legacy Source Field.**

## Other pending items (from memory index, unchanged today)
- LQ-310 ox/oy calibration pending (NGI + Grishma formats)
- Windows QZ raw printing: LQ310-RAW Generic/Text-Only queue fix pending (see `docs/claude_printing_handoff.md`)
- Live migrate will crash on hrms advance patch (currency precision memory)
- Sales Invoice audit open findings: VAT 0% API default, CBMS return retries, print-count race
