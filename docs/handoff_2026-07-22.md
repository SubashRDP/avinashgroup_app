# Handover — 2026-07-22

Master handover for the state of `avinashgroup_app` as of **2026-07-22 16:04** (commit `d7aa8b3`).
Covers everything since the previous handoff (`docs/handoff_2026-07-17.md`) and indexes the
per-topic handover docs. Written for whoever picks this codebase up next.

## State of the repo

- Branch `develop`, **synced with `origin/develop`**, working tree **clean**. Nothing local, nothing unpushed.
- Working site is **`avinas1`** on the office bench `/home/dell/frappe-v15` (see `CLAUDE.md`).
  This doc was written from the laptop clone (`~/frappe-15`), so "verify on site" items below
  were **not** re-verified against live — do that from the office bench.
- ~120 commits since the last handoff (2026-07-17 → 2026-07-22), summarized by area below.

## ⚠ Deploy debt — do these on the live server before anything else

1. **`bench --site avinas1 migrate` is pending and load-bearing.**
   - `69e8bdb` added `is_synced` / `is_realtime` columns for CBMS. Until migrate runs, **every CBMS
     sync raises after IRD has already accepted the bill** — the worst failure mode (bill lodged
     with IRD, ERP thinks it failed).
   - `ee37f45` added `patches.txt` entry `add_version_ref_doctype_creation_index` (index on
     `tabVersion` for the Invoice Activity report). Needs migrate to apply.
   - **Known migrate hazard:** live migrate will crash on the hrms *advance* patch (currency
     precision 5 work — see memory / `docs` history). Plan for it; don't run migrate blind
     five minutes before close of business.
2. **Dynamic Approval notifications not yet deployed to ng-group** — built and committed
   (`93a57ef`, `e7e146c`, `59aa68c`, `a7dbb84`), synced to avinas + nepalgas; **ng-group pending**.
3. **Open flag from 2026-07-17 — still unverified:** a live Numbering Configuration rule was
   screenshotted with `Copy Legacy Number Upto = 2026-07-15` and `Legacy Source Field =
   base_in_words`. If that record is real, any doc backdated ≤ 2026-07-15 gets its voucher number
   copied from the **amount-in-words text**. Check the record on `avinas1` and clear/fix it.

## What changed since 2026-07-17, by area

### Print Bridge (Windows raw-print agent) — v0.3.3 → v0.5.0
The single biggest thread. QZ Tray is **gone**; Print Bridge is now the only raw transport (`b0ddcfe`).

- `44564a5` v0.3.3 — survive shutdown: sign-in trigger for the autostart task, dropped 72h task limit.
- `eb7f14e` v0.3.4 — autostart registered via `schtasks /XML`, not PowerShell (execution-policy-proof).
- `7105c5b` v0.3.5 — installs without the Epson driver present; queue creates itself on first print.
- `c4ed7e5` v0.4.0 — self-diagnosis in the desk UI, hidden until a misconfiguration appears.
- `90a7172` v0.4.1 — don't abort a print that is still setting up the queue.
- `c85b6d4` v0.5.0 — auto-creates the `LQ310-RAW` Generic/Text-Only queue even with **no** Epson driver installed. This closes the "LQ-310 silent on raw jobs from Windows" saga.
- `079f337` — multi-origin allow-list: one install serves all 4 site origins; `928fd80` CI smoke-tests the EXE against all 4.
- `64acda2` — **release slot gotcha:** `releases/latest` on the GitHub repo belongs to the **K40** tool, not Print Bridge. Docs/JS pin a tagged release; never point anything at `latest`.
- Docs: `print_bridge_deployment.md` (deploy + 4-site verification), `print_bridge_till_setup.md`
  (front-line till setup & the recurring install-block error), `print_bridge_windows_handover.md`
  (install & verify), and **`print_bridge_troubleshooting_handover.md`** (`d7aa8b3`, today — field
  troubleshooting catalogue; start there when a till says "printing doesn't work").

### Payment Entry — cheque overlay + voucher formats
- Cheque overlay format (`payment_entry_cheque`) calibrated on paper for **Global IME** cheques
  (`65d0dd2`, `8a8d771`, `bea077b`), vertical/rotated feed (`74fcc51`, `2ca72b3`), A4-carrier
  variant (`b17db96`). Latest tweak `68fa46d`: date 2mm left, cheque centred for the **LBP2900
  centre-fed tray**. Calibration grid via `&cheque_grid=1`. Guide: `payment_entry_cheque_print.md`.
- New print formats: **Nepal Gas Payment Voucher** (`0e1ad77`), **Payment Entry Voucher Details**
  (`a4c4600`) and **Voucher Details 2** (`83e65ef`). "Created by" line added across voucher
  formats (`6478471`).

### Journal Entry print
- BJV format added (`dd2db77`) then renamed → **Nepal Gas Journal Voucher** (`24be7e6`, `2a64b7d`).
- Reworked today: `9e892a6` (large rewrite, +293) and `9ce4623` (**Contra voucher** rendering in
  the same format).

### Reports
- **Invoice Activity Report**: date window locked to the current fiscal year (`ee37f45`,
  `9cfbe8f`); rows keyed by `posting_date`, cut off at CBMS *Send From Date* (`1a38325`); only
  issued invoices — draft/cancelled/deleted dropped (`33e3700`, `96acabd`); print-heading fetch
  fix (`a9a4fcb`); **Sales Return details** merged in (`ea29151`). Note from memory: a full-FY
  run takes ~2 min.
- **Sales Register Report**: must pick a **Company** before it queries — prompt instead of an
  empty table (`caf275e`, `7ddea1b`, `b4a46de`).
- **Materialized / Materialized Return**: running-FY filter plumbing (`53f3ecf` via
  `CBMS/utils.py`), *Reason For Return* column (`cd630b1`).
- FY filter defaults now shared across cbms_activity, invoice_activity, materialized,
  materialized_return. **Site prerequisite:** FY `83/84` rows (all seven companies) must exist —
  they were missing on the site as of the report-FY work.

### CBMS / IRD
- `ce58185` — **operator-initiated backfill** (`custom_code/CBMS/backfill.py`) for Bills/Returns
  that never reached CBMS.
- `97a302b` — api_client + sales_invoice_hooks rework; docs updated in
  `docs/technical/05-cbms-integration.md`.
- `02dca9e` — **print-count reconcile tool** (`SalesInvoice/print_count.py`) to fix counter drift
  (the print-count race itself is still open, see below).
- Reminder: the `is_synced` migrate above is the blocking item.

### Dynamic Approval
- Per-flow configurable email notifications via Email Templates (`93a57ef`), optional Rejection
  notification (`e7e146c`), templates shipped as fixtures (`a7dbb84`), notify-creator + freeze
  rejected docs + stop native email leak (`59aa68c`). Guides: `dynamic_approval_guide.md`,
  `dynamic_approval_workflow.md`. Deploy state: avinas + nepalgas done, **ng-group pending**.

### Numbering
- Amendment voucher name pinned + fiscal year auto-filled from date (`88c1568`).
- Manual mode hides the "Auto-fill Document No. only when" section (`d4e3498`); In/Not In
  condition values use a plain Data box (`52ac506`).
- Docs: `numbering_handover.md`, `numbering_configuration.md`, `document_numbering*.md`.
- Tests: `bench run-tests` bootstrap fails (Holiday List); run direct unittest from `sites/`
  (see `docs`/memory *Running numbering tests*).

### Sales Invoice
- **Posting date + miti locked read-only for everyone** (`f4f952d`) — miti derived server-side
  from posting_date; old invoices not backfilled.
- Cancel/Delete hiding: built, shown at the ABMS visit, then **fully reverted** (`c1122ed`,
  2026-07-19). To restore: `git revert c1122ed`.
- UOM/item details refresh on item change moved to `app_include` JS (`87f236d`).
- VAT amount shown in UI before save (`627d64b`).

### Supplier Quotation Comparison
- Rebuilt to drive from **Purchase Order** (`73f5909`), RFQ path dropped; earlier Material
  Request filter (`ca25d21`) superseded — `material_request.js` hook removed.

### Misc
- Purchase Invoice print format (`39c3367`, A5 avinash), footer conditions (`6d5da9a`),
  company-filter doctypes fixture (`74e994c`), Grihalaxmi tweaks (`c4ef634`).

## Open items (carried + new)

| # | Item | Where |
| --- | --- | --- |
| 1 | Run migrate on live (`is_synced` cols + Version index patch); brace for hrms advance patch crash | live server |
| 2 | Verify/fix the `base_in_words` legacy-source Numbering Configuration record | avinas1 desk |
| 3 | Deploy Dynamic Approval notifications to ng-group | ng-group site |
| 4 | LQ-310 `ox/oy` print calibration (NGI + Grishma formats) | paper test at till |
| 5 | Create FY `83/84` (all 7 company rows) before Shrawan roll-over — naming throws without it | avinas1 desk |
| 6 | Sales Invoice audit open findings: VAT 0% API default, CBMS return retries, print-count race | `SalesInvoice/` |
| 7 | Submitted SI opens `__unsaved` with no field diffs (hides Cancel) — not root-caused | desk JS |
| 8 | Workflow submits don't auto-print (Company Print Template print-on-submit gap) | print template code |

## Doc index (start here for any given topic)

| Topic | Doc |
| --- | --- |
| Print Bridge — field troubleshooting | `print_bridge_troubleshooting_handover.md` |
| Print Bridge — install/verify on Windows | `print_bridge_windows_handover.md` |
| Print Bridge — deploy + 4-site checklist | `print_bridge_deployment.md` |
| Print Bridge — till setup for front-line staff | `print_bridge_till_setup.md` |
| Cheque printing | `payment_entry_cheque_print.md` |
| Numbering — handover | `numbering_handover.md` |
| Numbering — config / developer / user | `numbering_configuration.md`, `document_numbering_developer.md`, `document_numbering.md` |
| Dynamic Approval | `dynamic_approval_guide.md`, `dynamic_approval_workflow.md` |
| CBMS integration | `technical/05-cbms-integration.md` |
| DB replication runbook | `db-master-slave-replication.md` |
| Previous session handoffs | `handoff_2026-07-17.md`, `session_handoff_2026-07-15_printing.md`, `claude_printing_handoff.md` |
